# ARMv4T Rust-example project

## Setup

 1. `rustup update nightly`
 -  `rustup default nightly`
 -  `rustup component add rust-src`
 -  `cargo install xargo`
 -  `xargo build --target armv4t-none-eabi`